apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: chaos-n5-network
  namespace: hashed-network
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - n5-chaos.hashed.systems
    secretName: chaos-n5-network
  rules:
  - host: n5-chaos.hashed.systems
    http:
      paths:
      - pathType: Prefix
        path: /p2p/
        backend:
          service:
            name: chaos-n5-network
            port:
              number: 30333
      - pathType: Prefix
        path: /
        backend:
          service:
            name: chaos-n5-network
            port:
              number: 9944
      - pathType: Prefix
        path: /rpc/
        backend:
          service:
            name: chaos-n5-network
            port:
              number: 9933
